# Copy to .streamlit/secrets.toml for local dev, or paste into
# Streamlit Cloud's Settings → Secrets. The app falls back to an in-memory
# sample dataset if these are absent.

SUPABASE_URL = "https://xxxxx.supabase.co"
SUPABASE_KEY = "eyJ..."  # anon key (reads only)
# SUPABASE_SERVICE_ROLE_KEY = "eyJ..."   # only needed for the scraper
